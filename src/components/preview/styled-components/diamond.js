import styled from 'styled-components';

 const Diamond = styled.div`
   width: 1.5vw;
   height: 1.5vw;
   box-shadow: 0.1vw 0.075vw rgba(0, 0, 0, 0.3);
   background-color: var(--blue);
   transform: rotate(45deg);
`;

export { Diamond }
