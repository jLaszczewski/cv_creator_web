import styled from 'styled-components';

 const Row = styled.div`
  position: relative;
   display: flex;
   flex-direction: row;
   width: 89.26vw;
   margin-top: 1.9vw;
   height: max-content;
   justify-content: space-between;
   margin: auto;
   margin-bottom: 5vw;
   break-inside: avoid;
`;

export { Row }
